# Asset Pipeline (Placeholder)

## Sprite Pack Layout
```
assets/
  sprite_packs/
    default/
      metadata.json
      idle/
      happy/
      sad/
      sick/
      sleep/
      eat/
      study/
      play/
      work/
      family/
```

## Notes
- `metadata.json` should define frame counts, FPS, and stage overrides.
- Additional sprite packs can be dropped into `sprite_packs/` and selected via settings.
